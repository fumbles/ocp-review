// ═══════════════════════════════════════════════════════════════════════════════
// FLASHCARD DATA  (MC format)
// q: question, opts: [A,B,C,D], correct: 0-based index,
// explanation: shown after answer, doc: URL, docLabel: link text, tag: category
// ═══════════════════════════════════════════════════════════════════════════════
const allCards = [
// ── K8s Core ──────────────────────────────────────────────────────────────────
{tag:"core",
 q:"What is the smallest deployable unit in Kubernetes?",
 opts:["Container","Pod","ReplicaSet","Namespace"],
 correct:1,
 explanation:"A Pod wraps one or more containers that share a network namespace (same IP address) and can share storage volumes. Containers inside a Pod communicate over localhost. Pods are ephemeral — they are not updated in place but replaced.",
 doc:"https://kubernetes.io/docs/concepts/workloads/pods/",docLabel:"K8s Docs: Pods"},

{tag:"core",
 q:"Which control plane component is responsible for assigning Pods to Nodes?",
 opts:["kube-apiserver","kube-controller-manager","kube-scheduler","kubelet"],
 correct:2,
 explanation:"The kube-scheduler watches for unscheduled Pods and selects the best Node based on resource requests, taints/tolerations, affinity rules, and available capacity. It picks the node but does NOT start the Pod — the kubelet on that node does.",
 doc:"https://kubernetes.io/docs/concepts/scheduling-eviction/kube-scheduler/",docLabel:"K8s Docs: Scheduler"},

{tag:"core",
 q:"What does etcd store in a Kubernetes cluster?",
 opts:["Container image layers","The entire cluster state (all resource definitions and statuses)","Node operating system logs","Network packet routing tables"],
 correct:1,
 explanation:"etcd is a distributed key-value store and the single source of truth for the cluster. Every object — Pods, Services, Secrets, Deployments — is persisted in etcd. It uses the Raft consensus algorithm. Regular etcd backups are critical for disaster recovery.",
 doc:"https://kubernetes.io/docs/tasks/administer-cluster/configure-upgrade-etcd/",docLabel:"K8s Docs: etcd"},

{tag:"core",
 q:"What happens when a container exceeds its memory limit in Kubernetes?",
 opts:["It is throttled and slowed down","It is OOMKilled (terminated with exit code 137)","The pod is evicted from the node","The request is queued until memory is available"],
 correct:1,
 explanation:"CPU is compressible — exceeding the CPU limit causes throttling. Memory is not compressible — exceeding the memory limit causes the container to be OOMKilled (Out of Memory Kill) by the kernel with exit code 137. The kubelet then restarts it based on the restartPolicy.",
 doc:"https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/",docLabel:"K8s Docs: Resource Management"},

{tag:"core",
 q:"What is the purpose of a readinessProbe?",
 opts:["Restart a container that has deadlocked","Prevent traffic from reaching a pod that isn't ready","Delay startup of slow-initializing containers","Define the port the container listens on"],
 correct:1,
 explanation:"If a readinessProbe fails, the pod is removed from the Service's endpoint list — no traffic is routed to it. The pod is NOT restarted. This is essential for zero-downtime rolling updates and for apps with slow startup dependencies. The livenessProbe restarts containers; the startupProbe handles slow startup.",
 doc:"https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/",docLabel:"K8s Docs: Probes"},

{tag:"core",
 q:"What does a livenessProbe do when it fails?",
 opts:["Removes the pod from service endpoints","Triggers a rollback of the deployment","Restarts the container","Drains the node"],
 correct:2,
 explanation:"A livenessProbe determines whether a container is still alive. If it fails consecutively (failureThreshold times), the kubelet kills and restarts the container. Use it to recover from deadlocks. It does NOT remove the pod from service endpoints — that is the readinessProbe's job.",
 doc:"https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/",docLabel:"K8s Docs: Probes"},

{tag:"core",
 q:"What is the difference between a Deployment and a ReplicaSet?",
 opts:["A ReplicaSet can do rolling updates; a Deployment cannot","A Deployment manages ReplicaSets and enables rolling updates and rollbacks; a ReplicaSet only maintains pod count","They are identical — Deployment is just an alias","A Deployment runs on the control plane; a ReplicaSet runs on workers"],
 correct:1,
 explanation:"A ReplicaSet ensures N replicas of a pod run at all times. A Deployment manages ReplicaSets — it creates a new RS on each update, gradually scales it up, and scales down the old RS. This enables rolling updates, rollback history, and pause/resume. Always use Deployments, not ReplicaSets directly.",
 doc:"https://kubernetes.io/docs/concepts/workloads/controllers/deployment/",docLabel:"K8s Docs: Deployments"},

{tag:"core",
 q:"What is a DaemonSet used for?",
 opts:["Running a fixed number of replicas for high availability","Running exactly one pod on every (or selected) node","Scheduling batch jobs on a cron schedule","Providing stable network identities to stateful pods"],
 correct:1,
 explanation:"A DaemonSet ensures one Pod per matching node. As new nodes join the cluster, the DaemonSet automatically adds a pod to them. Common uses: log collectors (Fluentd), monitoring agents (Prometheus node exporter), network plugins (CNI), storage daemons.",
 doc:"https://kubernetes.io/docs/concepts/workloads/controllers/daemonset/",docLabel:"K8s Docs: DaemonSet"},

{tag:"core",
 q:"What makes a StatefulSet different from a Deployment?",
 opts:["StatefulSets can only run a single replica","StatefulSets provide stable pod names, stable network IDs, and ordered deployment/scaling","StatefulSets do not support persistent volumes","StatefulSets are only available in OpenShift, not vanilla Kubernetes"],
 correct:1,
 explanation:"StatefulSets are for stateful applications (databases, distributed systems). They provide: stable persistent hostnames (pod-0, pod-1), stable DNS names per pod, ordered graceful deployment/termination, and per-pod PVCs. Deployments treat pods as interchangeable — StatefulSets do not.",
 doc:"https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/",docLabel:"K8s Docs: StatefulSet"},

{tag:"core",
 q:"What is a ConfigMap?",
 opts:["A resource for storing encrypted database credentials","A resource for storing non-sensitive key-value configuration data","A resource for defining container resource limits","A resource for storing container images"],
 correct:1,
 explanation:"ConfigMaps store non-sensitive configuration as key-value pairs or file content. They are injected into pods as environment variables (envFrom/valueFrom) or mounted as files (volume). Values are stored in plain text — never use ConfigMaps for passwords or tokens. Use Secrets for sensitive data.",
 doc:"https://kubernetes.io/docs/concepts/configuration/configmap/",docLabel:"K8s Docs: ConfigMap"},

{tag:"core",
 q:"How are Secret values stored in etcd by default?",
 opts:["AES-256 encrypted","Base64-encoded (not encrypted)","SHA-256 hashed","Stored as plain text"],
 correct:1,
 explanation:"By default, Secret values are only base64-encoded, which is an encoding — not encryption. Anyone with access to etcd or RBAC permission to 'get' secrets can decode them with 'base64 -d'. For real protection, enable etcd encryption at rest and use RBAC to restrict Secret access.",
 doc:"https://kubernetes.io/docs/concepts/configuration/secret/",docLabel:"K8s Docs: Secrets"},

{tag:"core",
 q:"What is a ServiceAccount in Kubernetes?",
 opts:["A user account for cluster administrators","An identity for processes running inside pods to authenticate to the Kubernetes API","A network account that defines firewall rules","A storage account for pod volumes"],
 correct:1,
 explanation:"ServiceAccounts are identities for workloads (not humans). Every pod runs as a ServiceAccount (default: 'default'). Grant RBAC roles to ServiceAccounts when pods need to call the Kubernetes API — for example, an operator reading Deployments or a CI tool creating resources. Use the principle of least privilege.",
 doc:"https://kubernetes.io/docs/concepts/security/service-accounts/",docLabel:"K8s Docs: Service Accounts"},

{tag:"core",
 q:"What is a PodDisruptionBudget (PDB)?",
 opts:["A limit on how much CPU a pod can consume","A policy that limits how many pods can be voluntarily disrupted at once","A quota that restricts total pods per namespace","A rule that prevents pods from being scheduled on certain nodes"],
 correct:1,
 explanation:"A PDB protects applications during voluntary disruptions like node drains, cluster upgrades, or rolling deployments. Define minAvailable (e.g., 2 pods must always be running) or maxUnavailable (e.g., at most 1 can be down). The cluster upgrade process respects PDBs before draining nodes.",
 doc:"https://kubernetes.io/docs/tasks/run-application/configure-pdb/",docLabel:"K8s Docs: PodDisruptionBudget"},

{tag:"core",
 q:"What does 'emptyDir' volume type do?",
 opts:["Mounts a pre-existing host directory","Creates a temporary directory shared between containers in a pod; deleted when the pod is removed","Provisions a new PersistentVolumeClaim automatically","Mounts a ConfigMap as a read-only filesystem"],
 correct:1,
 explanation:"emptyDir creates a temporary directory on the node that starts empty when the pod is assigned to a node. All containers in the pod can read/write to it. The data is deleted when the pod is removed from the node. Use cases: scratch space, sharing files between sidecar and main container.",
 doc:"https://kubernetes.io/docs/concepts/storage/volumes/#emptydir",docLabel:"K8s Docs: emptyDir"},

{tag:"core",
 q:"What pod phase means all containers have exited with status 0?",
 opts:["Completed","Running","Succeeded","Terminated"],
 correct:2,
 explanation:"Pod phases: Pending (not yet running), Running (≥1 container active), Succeeded (all exited 0 — typical for Jobs), Failed (all exited, ≥1 non-zero), Unknown (node communication failure). CrashLoopBackOff is a container state, not a pod phase — it appears in the STATUS column of 'oc get pods'.",
 doc:"https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/",docLabel:"K8s Docs: Pod Lifecycle"},

{tag:"core",
 q:"What is the role of kube-proxy on each node?",
 opts:["Pull container images from registries","Schedule pods to nodes","Maintain iptables/IPVS rules that implement Kubernetes Service virtual IPs","Manage container lifecycle via CRI"],
 correct:2,
 explanation:"kube-proxy watches Services and Endpoints and programs iptables (or IPVS) rules on each node to route Service virtual IP traffic to the correct pod IPs. This is how 'ClusterIP:80' routes to one of the backend pods. In OpenShift 4.x, OVN-Kubernetes often replaces kube-proxy functionality.",
 doc:"https://kubernetes.io/docs/reference/command-line-tools-reference/kube-proxy/",docLabel:"K8s Docs: kube-proxy"},

{tag:"core",
 q:"Which field in a pod spec sets the CPU/memory a container is guaranteed?",
 opts:["limits","resources.guaranteed","requests","allocations"],
 correct:2,
 explanation:"'requests' is what the scheduler uses to find a node with enough capacity and what the pod is guaranteed. 'limits' is the ceiling — CPU throttles, memory OOMKills at the limit. Setting only limits without requests copies limits to requests. Always set both for predictable scheduling and autoscaling.",
 doc:"https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/",docLabel:"K8s Docs: Resource Requests & Limits"},

// ── OpenShift ─────────────────────────────────────────────────────────────────
{tag:"ocp",
 q:"How does an OpenShift Project differ from a Kubernetes Namespace?",
 opts:["Projects support multiple teams; Namespaces do not","A Project is a Namespace with extra metadata (display name, requester) and additional default RBAC/policies","Projects are cluster-scoped; Namespaces are node-scoped","They are identical — 'project' is just an alias in oc"],
 correct:1,
 explanation:"When you run 'oc new-project', OpenShift creates both a Kubernetes Namespace and a Project object with metadata like requester, displayName, and description. OpenShift also applies default NetworkPolicies and LimitRanges. 'kubectl create namespace' only creates the namespace with no extra resources.",
 doc:"https://docs.openshift.com/container-platform/4.18/applications/projects/working-with-projects.html",docLabel:"OpenShift Docs: Projects"},

{tag:"ocp",
 q:"What TLS termination mode decrypts at the router and re-encrypts traffic to the pod?",
 opts:["edge","passthrough","reencrypt","mutual"],
 correct:2,
 explanation:"Reencrypt: the router (HAProxy) terminates the client TLS connection and opens a new TLS connection to the pod — two separate TLS sessions. Edge: router decrypts, pod receives plain HTTP. Passthrough: router passes encrypted traffic directly to the pod without decrypting — the pod handles TLS itself.",
 doc:"https://docs.openshift.com/container-platform/4.18/networking/routes/secured-routes.html",docLabel:"OpenShift Docs: Secured Routes"},

{tag:"ocp",
 q:"What does 'oc new-app python:3.11~https://github.com/org/repo.git' do?",
 opts:["Deploys a pre-built image from quay.io","Uses Source-to-Image (S2I) to build the app from source and deploy it","Clones the repo and creates a ConfigMap from it","Creates a CronJob that syncs the git repo periodically"],
 correct:1,
 explanation:"The ~ syntax triggers Source-to-Image (S2I). OpenShift pulls the python:3.11 builder image, injects the source code from the git repo, builds a new image, and pushes it to the internal registry. It also creates a BuildConfig, ImageStream, Deployment, and Service automatically.",
 doc:"https://docs.openshift.com/container-platform/4.18/cicd/builds/build-strategies.html",docLabel:"OpenShift Docs: S2I Build Strategy"},

{tag:"ocp",
 q:"What is an ImageStream in OpenShift?",
 opts:["A high-throughput network connection between nodes","An abstraction that tracks multiple versions of container images and can trigger deployments on image updates","A log stream aggregated from all containers in a namespace","A build pipeline definition similar to a Jenkinsfile"],
 correct:1,
 explanation:"An ImageStream (IS) stores metadata (SHA digests, tags) for images, not the image layers themselves. Deployments can reference an ImageStream tag instead of a registry URL. When a new image is pushed to that tag, an image change trigger can automatically roll out a new deployment.",
 doc:"https://docs.openshift.com/container-platform/4.18/openshift_images/image-streams-manage.html",docLabel:"OpenShift Docs: ImageStreams"},

{tag:"ocp",
 q:"What is the default internal image registry address in OpenShift 4?",
 opts:["docker.io/openshift","registry.redhat.io","image-registry.openshift-image-registry.svc:5000","quay.io/openshift"],
 correct:2,
 explanation:"The OpenShift built-in registry is accessible within the cluster at image-registry.openshift-image-registry.svc:5000. S2I builds push to it automatically using the build service account credentials. Pods pull from it using their service account's image pull secret. It can also be exposed externally via a Route.",
 doc:"https://docs.openshift.com/container-platform/4.18/registry/index.html",docLabel:"OpenShift Docs: Image Registry"},

{tag:"ocp",
 q:"Which OpenShift RBAC role allows a user to manage all resources in a project INCLUDING granting access to others?",
 opts:["edit","view","admin","cluster-admin"],
 correct:2,
 explanation:"'admin' role: full control within a project, including granting roles to others. 'edit': CRUD on workloads but cannot manage roles or rolebindings. 'view': read-only. 'cluster-admin': cluster-wide admin — avoid for project-level tasks. Grant roles with: oc adm policy add-role-to-user admin username",
 doc:"https://docs.openshift.com/container-platform/4.18/authentication/using-rbac.html",docLabel:"OpenShift Docs: RBAC"},

{tag:"ocp",
 q:"What command grants a role to a ServiceAccount in OpenShift?",
 opts:["oc adm policy add-role-to-user view -z mysa","oc create rolebinding --serviceaccount mysa --role view","oc grant role view serviceaccount/mysa","oc auth bind view --sa mysa"],
 correct:0,
 explanation:"The -z flag specifies a ServiceAccount name (instead of a user). Example: 'oc adm policy add-role-to-user view -z my-sa' grants the view role to the my-sa ServiceAccount in the current project. Behind the scenes, this creates a RoleBinding.",
 doc:"https://docs.openshift.com/container-platform/4.18/authentication/using-rbac.html",docLabel:"OpenShift Docs: RBAC"},

{tag:"ocp",
 q:"What is a DeploymentConfig and when should you use it?",
 opts:["The recommended way to deploy all apps in OCP 4.18","An OpenShift-specific predecessor to Deployments with image/config change triggers; prefer Deployments in OCP 4.14+","A deployment strategy that only works with StatefulSets","A multi-cluster deployment resource"],
 correct:1,
 explanation:"DeploymentConfigs (dc) predate Kubernetes Deployments and add triggers (image change, config change) and lifecycle hooks. Since OCP 4.14, standard Kubernetes Deployments are preferred — they have first-class support, work with kubectl, and are the upstream standard. Migrate existing DCs to Deployments when possible.",
 doc:"https://docs.openshift.com/container-platform/4.18/applications/deployments/what-deployments-are.html",docLabel:"OpenShift Docs: Deployments vs DeploymentConfigs"},

{tag:"ocp",
 q:"What command would you use to add a private registry pull secret to the 'default' service account?",
 opts:["oc set imagepullsecret default regcred","oc secrets link default regcred --for=pull","oc adm policy add-secret regcred default","oc create secret mount regcred --sa=default"],
 correct:1,
 explanation:"After creating the pull secret (oc create secret docker-registry regcred ...), link it to the service account: 'oc secrets link default regcred --for=pull'. This lets pods running as the 'default' SA pull from the private registry. Use --for=pull for image pulls, or --for=mount for volume-mounted secrets.",
 doc:"https://docs.openshift.com/container-platform/4.18/openshift_images/managing_images/using-image-pull-secrets.html",docLabel:"OpenShift Docs: Pull Secrets"},

// ── Networking ────────────────────────────────────────────────────────────────
{tag:"networking",
 q:"What Service type exposes an application only within the cluster?",
 opts:["NodePort","LoadBalancer","ClusterIP","ExternalName"],
 correct:2,
 explanation:"ClusterIP (the default) assigns a virtual IP reachable only from within the cluster. Other types build on top of it: NodePort adds node-level exposure, LoadBalancer adds external cloud LB provisioning. ExternalName is a DNS alias. For OpenShift external access, use Routes instead of NodePort/LoadBalancer.",
 doc:"https://kubernetes.io/docs/concepts/services-networking/service/",docLabel:"K8s Docs: Service Types"},

{tag:"networking",
 q:"What is the full DNS name for a Service named 'api' in namespace 'backend'?",
 opts:["api.backend.cluster.local","api.backend.svc.cluster.local","backend.api.svc.cluster.local","api.svc.backend.cluster.local"],
 correct:1,
 explanation:"The Kubernetes DNS pattern is: &lt;service&gt;.&lt;namespace&gt;.svc.cluster.local. From the same namespace, you can shorten it to just 'api'. From another namespace, use 'api.backend' or the full FQDN. The cluster.local domain is the default cluster domain.",
 doc:"https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/",docLabel:"K8s Docs: DNS for Services"},

{tag:"networking",
 q:"How does a Kubernetes Service discover which pods to send traffic to?",
 opts:["By pod name prefix","Via label selectors that match pod labels","By IP address range","By pod restart count"],
 correct:1,
 explanation:"Services use label selectors (e.g., app: webapp) to find matching pods. The Endpoints controller continuously updates the Endpoints object with the IPs of Ready pods that match the selector. Check with: 'oc get endpoints &lt;svc-name&gt;'. If Endpoints is empty, the selector doesn't match any Running+Ready pods.",
 doc:"https://kubernetes.io/docs/concepts/services-networking/service/#defining-a-service",docLabel:"K8s Docs: Service Selectors"},

{tag:"networking",
 q:"What does 'oc expose svc/webapp' create?",
 opts:["A LoadBalancer service","A NodePort service","An OpenShift Route pointing to the Service","A Kubernetes Ingress resource"],
 correct:2,
 explanation:"'oc expose svc/&lt;name&gt;' creates an OpenShift Route that configures the cluster's HAProxy Ingress Router to forward external HTTP/HTTPS traffic to the Service. Routes support custom hostnames (--hostname), TLS termination, and path-based routing. Routes are the OpenShift-native way to expose apps externally.",
 doc:"https://docs.openshift.com/container-platform/4.18/networking/routes/route-configuration.html",docLabel:"OpenShift Docs: Routes"},

{tag:"networking",
 q:"What does a NetworkPolicy do in Kubernetes?",
 opts:["Defines pod CPU and memory limits","Controls which pods can communicate with each other using label selectors","Configures DNS resolution for Services","Sets up TLS certificates for ingress traffic"],
 correct:1,
 explanation:"NetworkPolicies are firewall rules for pods. They select pods with labels and define ingress (incoming) and egress (outgoing) traffic rules. By default in OpenShift, pods can talk to any other pod in any namespace. NetworkPolicies restrict this. If no NetworkPolicy selects a pod, it allows all traffic.",
 doc:"https://kubernetes.io/docs/concepts/services-networking/network-policies/",docLabel:"K8s Docs: NetworkPolicy"},

{tag:"networking",
 q:"What is the NodePort port range in Kubernetes?",
 opts:["1024–8080","8080–9090","30000–32767","443–8443"],
 correct:2,
 explanation:"NodePort Services expose the app on a static port in the range 30000–32767 on every node's IP. Traffic reaches NodeIP:NodePort and is forwarded to the Service. Not ideal for production (non-standard ports, exposes on all nodes). In OpenShift, use Routes (backed by HAProxy) for external exposure instead.",
 doc:"https://kubernetes.io/docs/concepts/services-networking/service/#type-nodeport",docLabel:"K8s Docs: NodePort"},

{tag:"networking",
 q:"Which Route TLS mode leaves the router unable to inspect the traffic?",
 opts:["edge","reencrypt","passthrough","mutual-tls"],
 correct:2,
 explanation:"Passthrough: the Ingress Router forwards the raw TLS stream directly to the pod without decrypting it. The pod must handle TLS itself. The router cannot add headers, do path routing, or inspect traffic. Use when end-to-end encryption is required and the pod manages its own certificate.",
 doc:"https://docs.openshift.com/container-platform/4.18/networking/routes/secured-routes.html",docLabel:"OpenShift Docs: Passthrough Routes"},

// ── Storage ───────────────────────────────────────────────────────────────────
{tag:"storage",
 q:"What is a PersistentVolume (PV)?",
 opts:["A namespace-scoped storage request made by a pod","A cluster-level storage resource with its own lifecycle independent of any pod","A temporary volume that exists only for the pod's lifetime","A ConfigMap that stores file system paths"],
 correct:1,
 explanation:"PersistentVolumes are cluster-level resources (like Nodes). They are provisioned by admins or dynamically by StorageClasses. A PV has capacity, access modes, and a reclaim policy. Its lifecycle is independent of pods — data persists even after the pod is deleted. PVCs bind to PVs to use the storage.",
 doc:"https://kubernetes.io/docs/concepts/storage/persistent-volumes/",docLabel:"K8s Docs: Persistent Volumes"},

{tag:"storage",
 q:"What PVC access mode allows multiple nodes to read AND write simultaneously?",
 opts:["ReadWriteOnce (RWO)","ReadOnlyMany (ROX)","ReadWriteMany (RWX)","ReadWriteOncePod (RWOP)"],
 correct:2,
 explanation:"ReadWriteMany (RWX) allows multiple nodes to mount and write to the volume at the same time. It requires a storage backend that supports it — NFS, CephFS, or Azure Files. Block-based storage (AWS EBS, GCP PD) typically only supports RWO. Check your StorageClass for supported access modes.",
 doc:"https://kubernetes.io/docs/concepts/storage/persistent-volumes/#access-modes",docLabel:"K8s Docs: PVC Access Modes"},

{tag:"storage",
 q:"What happens to a PV when its PVC is deleted and the reclaim policy is 'Retain'?",
 opts:["The PV and its data are immediately deleted","The PV is scrubbed and made available for a new PVC","The PV remains with its data intact; manual cleanup is required before reuse","The PV is automatically rebound to another PVC"],
 correct:2,
 explanation:"With Retain policy: deleting the PVC releases the PV, but the PV and its data are preserved. The PV moves to 'Released' status. An admin must manually reclaim it — clean up the data, delete the PV object, and create a new one. With 'Delete' policy, the PV and underlying storage (e.g., cloud disk) are deleted.",
 doc:"https://kubernetes.io/docs/concepts/storage/persistent-volumes/#reclaiming",docLabel:"K8s Docs: PV Reclaim Policy"},

{tag:"storage",
 q:"What does a StorageClass enable?",
 opts:["Manual PV provisioning by cluster admins only","Dynamic provisioning of PersistentVolumes when a PVC is created","Defining IOPS limits for containers","Encrypting ConfigMap data at rest"],
 correct:1,
 explanation:"When a PVC specifies a StorageClass (or uses the default), the cluster's provisioner automatically creates a matching PV. This is dynamic provisioning — no admin needs to pre-create PVs. The StorageClass defines the provisioner plugin (e.g., aws-ebs, nfs.csi.k8s.io), reclaim policy, and volume binding mode.",
 doc:"https://kubernetes.io/docs/concepts/storage/storage-classes/",docLabel:"K8s Docs: StorageClass"},

{tag:"storage",
 q:"A PVC has been Pending for 10 minutes. What is the most likely cause?",
 opts:["The pod hasn't started yet","No PV matches the PVC's requirements, or no default StorageClass is configured","The PVC YAML syntax is invalid","The namespace doesn't have RBAC permission to use storage"],
 correct:1,
 explanation:"A Pending PVC means no PV was bound. Check: (1) 'oc describe pvc &lt;name&gt;' for events. (2) Does a PV exist with matching size, access mode, and storageClassName? (3) Is a default StorageClass configured (oc get storageclass)? (4) Is the dynamic provisioner pod healthy in the kube-system namespace?",
 doc:"https://kubernetes.io/docs/concepts/storage/persistent-volumes/#binding",docLabel:"K8s Docs: PV Binding"},

// ── HA & Reliability ──────────────────────────────────────────────────────────
{tag:"ha",
 q:"What is the difference between the 'Recreate' and 'RollingUpdate' deployment strategies?",
 opts:["Recreate is faster but causes downtime; RollingUpdate gradually replaces pods with zero downtime","RollingUpdate is only available with StatefulSets","Recreate uses blue/green switching; RollingUpdate uses canary","They are identical — only the name differs"],
 correct:0,
 explanation:"Recreate: terminates ALL existing pods first, then creates new ones. This causes downtime but avoids running two versions simultaneously (useful when versions are incompatible). RollingUpdate: gradually replaces pods controlled by maxUnavailable (pods down during update) and maxSurge (extra pods allowed). Default strategy.",
 doc:"https://kubernetes.io/docs/concepts/workloads/controllers/deployment/#strategy",docLabel:"K8s Docs: Deployment Strategies"},

{tag:"ha",
 q:"What does 'maxSurge: 1' mean in a RollingUpdate strategy?",
 opts:["At most 1 pod can be unavailable during the update","Exactly 1 pod is updated at a time","At most 1 extra pod above the desired count can exist during the update","The update must complete within 1 minute"],
 correct:2,
 explanation:"maxSurge controls how many pods above the desired replica count can be created during a rolling update. With maxSurge: 1 and 3 replicas, up to 4 pods can exist at once during the update. maxUnavailable controls how many pods can be down simultaneously. Set maxSurge to 0 for strict resource constraints.",
 doc:"https://kubernetes.io/docs/concepts/workloads/controllers/deployment/#rolling-update-deployment",docLabel:"K8s Docs: Rolling Update"},

{tag:"ha",
 q:"What command rolls back a Deployment to its previous version?",
 opts:["oc deploy rollback webapp","oc rollout undo deployment/webapp","oc set image deployment/webapp --revert","oc apply -f previous-version.yaml"],
 correct:1,
 explanation:"'oc rollout undo deployment/&lt;name&gt;' reverts to the previous ReplicaSet revision. To go to a specific revision: 'oc rollout undo deployment/&lt;name&gt; --to-revision=3'. View revision history: 'oc rollout history deployment/&lt;name&gt;'. Add --record to deployment commands to annotate history with the command used.",
 doc:"https://kubernetes.io/docs/concepts/workloads/controllers/deployment/#rolling-back-a-deployment",docLabel:"K8s Docs: Rollback"},

{tag:"ha",
 q:"What is required for a HorizontalPodAutoscaler (HPA) to function?",
 opts:["The cluster must have at least 3 nodes","Resource requests must be set on the target deployment's containers","The Metrics Server must be installed and resource limits must be set","The deployment must use a StatefulSet"],
 correct:1,
 explanation:"HPA scales based on the ratio of current usage to the resource request. Without requests set, the HPA cannot calculate utilization percentages and will not scale. The Metrics Server (or Prometheus Adapter for custom metrics) must also be installed. Create with: oc autoscale deployment/&lt;name&gt; --min=2 --max=10 --cpu-percent=70",
 doc:"https://kubernetes.io/docs/tasks/run-application/horizontal-pod-autoscale/",docLabel:"K8s Docs: HPA"},

{tag:"ha",
 q:"What is pod anti-affinity used for in a high-availability setup?",
 opts:["Preventing pods from using too much memory","Ensuring pods of the same app are spread across different nodes","Pinning specific pods to specific nodes","Preventing pods from being scheduled on tainted nodes"],
 correct:1,
 explanation:"Pod anti-affinity with topologyKey: kubernetes.io/hostname prevents multiple replicas of the same app from landing on the same node. If a node fails, you still have pods on other nodes. Use preferredDuringScheduling for soft preference (allows same-node if no alternative) or requiredDuringScheduling for a hard rule.",
 doc:"https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#affinity-and-anti-affinity",docLabel:"K8s Docs: Pod Anti-Affinity"},

{tag:"ha",
 q:"What does a startupProbe do that a livenessProbe cannot handle?",
 opts:["It monitors memory usage during startup","It delays liveness and readiness checks until a slow-starting container finishes initializing","It sends an alert when startup takes too long","It configures the restart policy for the container"],
 correct:1,
 explanation:"For containers that take a long time to start (e.g., Java apps, legacy systems), a livenessProbe would fail prematurely and cause endless restart loops. startupProbe disables liveness and readiness probes until it succeeds once. Set failureThreshold × periodSeconds to cover the max startup time. Once it passes, the livenessProbe takes over.",
 doc:"https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/#define-startup-probes",docLabel:"K8s Docs: Startup Probes"},

// ── CLI ───────────────────────────────────────────────────────────────────────
{tag:"cli",
 q:"How do you view logs from a container that has already crashed and restarted?",
 opts:["oc logs &lt;pod&gt; --all","oc logs &lt;pod&gt; --previous","oc describe pod &lt;pod&gt; --logs","oc get logs &lt;pod&gt; -c crashed"],
 correct:1,
 explanation:"'oc logs &lt;pod&gt; --previous' (or -p) shows the logs from the terminated previous container instance. This is the first thing to check when debugging CrashLoopBackOff — the logs reveal why it crashed before the kubelet restarted it. For a specific container in a multi-container pod: 'oc logs &lt;pod&gt; -c &lt;container&gt; --previous'",
 doc:"https://kubernetes.io/docs/concepts/cluster-administration/logging/",docLabel:"K8s Docs: Logging"},

{tag:"cli",
 q:"What is the difference between 'oc apply' and 'oc create'?",
 opts:["oc apply only works with YAML; oc create works with JSON","oc apply creates or updates (idempotent); oc create fails if the resource already exists","oc apply requires admin access; oc create does not","oc apply is for namespaced resources; oc create is for cluster-scoped resources"],
 correct:1,
 explanation:"'oc apply -f file.yaml' is idempotent — it creates the resource if it doesn't exist, or updates it to match the manifest. Ideal for GitOps. 'oc create -f file.yaml' creates only — returns an error if the resource already exists. Use 'oc create' for one-off imperative creation, 'oc apply' for declarative management.",
 doc:"https://kubernetes.io/docs/concepts/overview/working-with-objects/object-management/",docLabel:"K8s Docs: Object Management"},

{tag:"cli",
 q:"What does 'oc get pods -o wide' show that 'oc get pods' does not?",
 opts:["Container image names and environment variables","Node assignment, pod IP, and nominated node","Resource requests and limits","Volume mounts and ConfigMap references"],
 correct:1,
 explanation:"'-o wide' adds columns: NODE (which node the pod runs on), IP (pod IP address), NOMINATED NODE (preemption candidate), and READINESS GATES. Useful when debugging scheduling issues, node failures, or network connectivity problems. '-o yaml' gives the full resource definition.",
 doc:"https://kubernetes.io/docs/reference/kubectl/cheatsheet/",docLabel:"K8s Docs: kubectl Cheat Sheet"},

{tag:"cli",
 q:"What does 'oc explain pod.spec.containers.resources' do?",
 opts:["Opens an external browser with the API docs","Shows the inline API documentation for the resources field including its sub-fields and types","Lists all pods with resource limits set","Generates a sample YAML for resource configuration"],
 correct:1,
 explanation:"'oc explain' (or 'kubectl explain') is built-in API reference documentation. Navigate the resource schema with dot notation. 'oc explain pod' → top level fields. 'oc explain pod.spec' → spec fields. 'oc explain pod.spec.containers.resources' → the requests/limits fields. Use --recursive to see all nested fields at once.",
 doc:"https://kubernetes.io/docs/reference/kubectl/generated/kubectl_explain/",docLabel:"K8s Docs: kubectl explain"},

{tag:"cli",
 q:"What flag watches a resource for live changes in the terminal?",
 opts:["-f (follow)","--live","-w (watch)","--stream"],
 correct:2,
 explanation:"'-w' (--watch) keeps the command running and prints updates as resources change. 'oc get pods -w' shows pod status changes in real time — useful during rolling updates, deployments, or troubleshooting. Press Ctrl+C to stop. Combine with '-o wide' or '--field-selector' for more targeted watching.",
 doc:"https://kubernetes.io/docs/reference/kubectl/cheatsheet/",docLabel:"K8s Docs: kubectl Cheat Sheet"},

{tag:"cli",
 q:"What is 'oc debug deployment/webapp' used for?",
 opts:["Enables debug logging for all containers in the deployment","Starts a temporary interactive copy of the deployment's pod for troubleshooting, overriding the entrypoint with a shell","Opens the deployment YAML in edit mode","Runs a health check against all pods in the deployment"],
 correct:1,
 explanation:"'oc debug deployment/&lt;name&gt;' creates a temporary pod from the deployment's pod template, overrides the command with a shell (so CrashLoopBackOff apps don't crash before you can investigate). Inside you can run the app manually, inspect env vars, test connectivity. The pod is deleted when you exit the shell.",
 doc:"https://docs.openshift.com/container-platform/4.18/support/troubleshooting/investigating-pod-issues.html",docLabel:"OpenShift Docs: Troubleshooting Pods"},

{tag:"cli",
 q:"How do you switch to a different project in OpenShift?",
 opts:["oc use project &lt;name&gt;","oc switch &lt;name&gt;","oc project &lt;name&gt;","oc context set --project &lt;name&gt;"],
 correct:2,
 explanation:"'oc project &lt;name&gt;' switches your active context to the specified project. 'oc project' (no argument) shows the current project. 'oc get projects' lists all projects you have access to. In vanilla kubectl, the equivalent is: kubectl config set-context --current --namespace=&lt;namespace&gt;",
 doc:"https://docs.openshift.com/container-platform/4.18/cli_reference/openshift_cli/getting-started-cli.html",docLabel:"OpenShift Docs: oc CLI"},

{tag:"cli",
 q:"What command shows you all events in the current namespace sorted by time?",
 opts:["oc get events --sort-by='.lastTimestamp'","oc describe events --all","oc logs --events","oc get all --show-events"],
 correct:0,
 explanation:"'oc get events --sort-by=.lastTimestamp' (or kubectl equivalent) shows cluster events sorted by most recent. Events reveal scheduling failures, image pull errors, OOMKills, probe failures, and volume mount issues. Narrow to a specific resource: 'oc get events --field-selector involvedObject.name=&lt;pod-name&gt;'",
 doc:"https://kubernetes.io/docs/reference/kubectl/generated/kubectl_get/",docLabel:"K8s Docs: kubectl get"},

{tag:"cli",
 q:"What is the purpose of 'oc auth can-i get pods --as=alice'?",
 opts:["Logs in as alice to run the command","Tests whether the user alice has permission to get pods in the current namespace","Creates a pod as the alice service account","Grants alice get permissions on pods"],
 correct:1,
 explanation:"'oc auth can-i &lt;verb&gt; &lt;resource&gt; --as=&lt;user&gt;' impersonates a user to test their permissions without logging in as them. Returns 'yes' or 'no'. Use to audit RBAC: 'oc auth can-i --list --as=alice' shows everything alice can do. Requires impersonation rights (typically cluster-admin).",
 doc:"https://kubernetes.io/docs/reference/kubectl/generated/kubectl_auth/kubectl_auth_can-i/",docLabel:"K8s Docs: auth can-i"},

// ── Advanced / Expert ──────────────────────────────────────────────────────────
{tag:"advanced",
 q:"You need to debug a pod that keeps crashing before you can exec into it. What is the correct command?",
 opts:["oc exec -it &lt;pod&gt; -- /bin/bash","oc logs &lt;pod&gt; --previous","oc debug pod/&lt;name&gt;","oc rsh &lt;pod&gt;"],
 correct:2,
 explanation:"'oc debug pod/&lt;name&gt;' creates a copy of the pod spec with the entrypoint overridden, so you get a shell even if the original command crashes immediately. '--previous' shows logs from the last crashed instance but doesn't give you a shell. 'exec' and 'rsh' require the container to be running.",
 doc:"https://docs.openshift.com/container-platform/4.18/support/troubleshooting/investigating-pod-issues.html",docLabel:"OCP Docs: debug pod"},

{tag:"advanced",
 q:"What does 'oc port-forward svc/mydb 5432:5432' do that 'oc port-forward pod/mydb-0 5432:5432' does not?",
 opts:["It creates a Route for external access","It tunnels through the HAProxy ingress router","It load-balances across all pods selected by the Service","It opens the port on the node instead of localhost"],
 correct:2,
 explanation:"Port-forwarding to a Service selects a random healthy pod from the Service's endpoints on each connection — useful when you want to hit the service abstraction rather than a specific pod. Port-forwarding to a named pod always targets that exact pod. Neither creates a Route or goes through HAProxy — both create a direct tunnel from your local machine through the API server to the pod.",
 doc:"https://kubernetes.io/docs/tasks/access-application-cluster/port-forward-access-application-cluster/",docLabel:"K8s Docs: port-forward"},

{tag:"advanced",
 q:"A pod has exit code 137. What is the most likely cause and first debugging step?",
 opts:["Liveness probe failed — check probe config","OOMKilled — check 'oc adm top pod &lt;name&gt; --containers' vs memory limits","Image pull failure — check imagePullSecrets","SCC rejection — check 'oc get events'"],
 correct:1,
 explanation:"Exit code 137 = SIGKILL, which is how the Linux OOM killer terminates processes. The container exceeded its memory limit. Check actual usage with 'oc adm top pod --containers' vs the configured limit with 'oc get pod -o jsonpath='{.spec.containers[*].resources}'. Fix by raising the limit or using VPA. Exit code 1 = app error, 128+signal = signal kill.",
 doc:"https://docs.openshift.com/container-platform/4.18/nodes/clusters/nodes-cluster-resource-configure.html",docLabel:"OCP Docs: resource limits"},

{tag:"advanced",
 q:"Which command shows you exactly what would change in the cluster BEFORE running 'oc apply -f manifest.yaml'?",
 opts:["oc apply -f manifest.yaml --dry-run=client","oc apply -f manifest.yaml --dry-run=server","oc diff -f manifest.yaml","oc get -f manifest.yaml"],
 correct:2,
 explanation:"'oc diff -f manifest.yaml' compares the local manifest against the live cluster state and outputs a unified diff. '--dry-run=client' validates locally but doesn't check live state. '--dry-run=server' sends the request to the API server (catching webhook rejections and quota issues) but also doesn't show a diff. Use 'diff' to review changes, then '--dry-run=server' to validate, then 'apply'.",
 doc:"https://kubernetes.io/docs/concepts/cluster-administration/manage-deployment/",docLabel:"K8s Docs: managing deployments"},

{tag:"advanced",
 q:"What is the difference between '--dry-run=client' and '--dry-run=server'?",
 opts:["Client validates YAML syntax; server validates against the live API","Client skips all validation; server runs full validation","They are equivalent — both prevent changes","Client is faster; server is only needed for CRDs"],
 correct:0,
 explanation:"'--dry-run=client' validates the manifest locally against the OpenAPI schema but never contacts the cluster — it won't catch quota violations, webhook rejections, or missing CRDs. '--dry-run=server' sends the full request to the API server (including admission webhooks and quota checks) but instructs it not to persist — it catches real-world rejections without making changes. Always prefer server-side dry-run in production pipelines.",
 doc:"https://kubernetes.io/docs/reference/using-api/server-side-apply/",docLabel:"K8s Docs: server-side apply"},

{tag:"advanced",
 q:"You want to stream logs from ALL pods with label 'app=api' simultaneously. Which command is correct?",
 opts:["oc logs -l app=api","oc logs -l app=api -f","oc logs -l app=api --all-containers=true -f --max-log-requests=10","oc logs deployment/api -f"],
 correct:2,
 explanation:"When using a label selector with 'oc logs', you must add '--all-containers=true' to include all containers per pod and '--max-log-requests=N' to set the concurrency limit (default is 5). Without these flags you may miss containers or hit the default request cap. 'oc logs deployment/api -f' only tails one pod selected by the Deployment, not all of them.",
 doc:"https://kubernetes.io/docs/reference/kubectl/generated/kubectl_logs/",docLabel:"K8s Docs: logs"},

{tag:"advanced",
 q:"How do you force-delete a pod stuck in 'Terminating' state after its grace period has elapsed?",
 opts:["oc delete pod &lt;name&gt; --now","oc delete pod &lt;name&gt; --grace-period=0 --force","oc patch pod &lt;name&gt; -p '{\"metadata\":{\"finalizers\":[]}}'","Both B and C can work depending on whether finalizers are set"],
 correct:3,
 explanation:"If a pod is stuck Terminating due to expired grace period, '--grace-period=0 --force' sends SIGKILL immediately and removes the API object. However, if it's stuck due to a finalizer that a controller hasn't processed, you need to remove the finalizer with 'oc patch'. A pod can be stuck for BOTH reasons — check 'oc describe pod' for finalizers first.",
 doc:"https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/",docLabel:"K8s Docs: pod lifecycle"},

{tag:"advanced",
 q:"Which command correctly generates a short-lived token for a ServiceAccount for use in scripts?",
 opts:["oc sa get-token my-sa","oc create token my-sa --duration=1h","oc get secret -o jsonpath='{.data.token}' | base64 -d","oc serviceaccount token my-sa"],
 correct:1,
 explanation:"'oc create token &lt;sa&gt; --duration=&lt;time&gt;' creates a time-bound token using the TokenRequest API (Kubernetes 1.22+). The old approach of reading from a long-lived secret (option C) still works but those tokens don't expire and are a security risk. 'oc sa get-token' was an older OpenShift-specific command that has been deprecated.",
 doc:"https://kubernetes.io/docs/reference/access-authn-authz/service-accounts-admin/",docLabel:"K8s Docs: ServiceAccount tokens"},

{tag:"advanced",
 q:"What does 'oc get events --sort-by=.lastTimestamp --field-selector type=Warning' show you?",
 opts:["All events sorted alphabetically","Warning events sorted by most recent — the fastest way to spot cluster problems","All Warning events across all namespaces","Cluster-level audit log entries"],
 correct:1,
 explanation:"Events are the fastest triage tool in Kubernetes. '--sort-by=.lastTimestamp' shows newest events last (most useful in terminal). '--field-selector type=Warning' filters to only Warning events, cutting out informational noise. This single command surfaces OOMKills, failed scheduling, image pull errors, probe failures, and SCC rejections faster than almost any other approach.",
 doc:"https://kubernetes.io/docs/reference/kubectl/generated/kubectl_get/",docLabel:"K8s Docs: get events"},

{tag:"advanced",
 q:"A pod is Pending. 'oc describe pod' shows 'Insufficient cpu'. What are your two main options?",
 opts:["Delete the pod and recreate it; or add more nodes","Lower the pod's CPU request; or add capacity (scale nodes/enable cluster autoscaler)","Delete other pods on the node; or increase the CPU limit","Switch from Deployment to DaemonSet; or remove the resourceQuota"],
 correct:1,
 explanation:"'Insufficient cpu' means the scheduler cannot find a node with enough allocatable CPU to satisfy the pod's request. Option 1: reduce the pod's CPU request if it's over-estimated. Option 2: add capacity — scale up the MachineSet, enable the Cluster Autoscaler, or move other workloads off nodes. Never lower limits to fix scheduling — limits don't affect scheduling, only requests do.",
 doc:"https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/",docLabel:"K8s Docs: resource requests"},

{tag:"advanced",
 q:"What is the correct way to check which cluster operators are degraded before a cluster upgrade?",
 opts:["oc get clusterversion -o yaml","oc get co | grep -v 'True.*False.*False'","oc adm upgrade --check","oc get clusteroperators --field-selector=status.conditions.type=Degraded"],
 correct:1,
 explanation:"'oc get co' shows all ClusterOperators with three condition columns: Available, Progressing, Degraded. A healthy operator is True/False/False. The grep inverts that — showing anything that isn't fully healthy (degraded, unavailable, or still progressing). A degraded ClusterOperator will BLOCK a cluster upgrade, so this check is mandatory before 'oc adm upgrade'.",
 doc:"https://docs.openshift.com/container-platform/4.18/updating/preparing_for_updates/updating-cluster-prepare.html",docLabel:"OCP Docs: pre-upgrade checks"},

{tag:"advanced",
 q:"How do you open a root shell on an OpenShift node without SSH access?",
 opts:["oc exec -it node/&lt;name&gt; -- bash","oc debug node/&lt;name&gt;, then 'chroot /host'","oc rsh node/&lt;name&gt;","oc adm node-shell &lt;name&gt;"],
 correct:1,
 explanation:"'oc debug node/&lt;name&gt;' schedules a privileged pod on the target node with the host filesystem mounted at /host. After the shell opens, run 'chroot /host' to switch to the host root — giving you access to systemctl, journalctl, crictl, the host network stack, and all files as if you had SSH'd in. This is the standard OCP node access method since nodes run immutable RHCOS.",
 doc:"https://docs.openshift.com/container-platform/4.18/support/troubleshooting/verifying-node-health.html",docLabel:"OCP Docs: node debugging"},

{tag:"advanced",
 q:"'oc adm must-gather' is used for what purpose?",
 opts:["Gathering all container logs in a namespace","Collecting cluster-wide diagnostic data (logs, configs, events, resource state) for support cases","Running a pre-upgrade health check","Backing up etcd"],
 correct:1,
 explanation:"'oc adm must-gather' runs a privileged pod that collects logs from all operators, resource manifests, cluster version info, events, and API server state into a local tar archive. It's the first thing Red Hat support asks for. Run: 'oc adm must-gather --dest-dir=./must-gather-$(date +%F)'. You can also target specific operators: '--image=\$(oc -n openshift-storage get csv -o jsonpath=...) '",
 doc:"https://docs.openshift.com/container-platform/4.18/support/gathering-cluster-data.html",docLabel:"OCP Docs: must-gather"},
];
